<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- 投票记录映射文件 -->
<mapper namespace="mapper.VoteRecordMapper">
    <!-- 投票记录结果映射 -->
    <resultMap id="BaseResultMap" type="bean.VoteRecord">
        <id column="vote_id" jdbcType="INTEGER" property="voteId"/>
        <result column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="candidate_id" jdbcType="INTEGER" property="candidateId"/>
        <result column="vote_time" jdbcType="TIMESTAMP" property="voteTime"/>
    </resultMap>

    <!-- Example查询条件 -->
    <sql id="Example_Where_Clause">
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="("
                                             separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>

    <!-- 更新ByExample查询条件 -->
    <sql id="Update_By_Example_Where_Clause">
        <where>
            <foreach collection="example.oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="("
                                             separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>

    <!-- 基本列列表 -->
    <sql id="Base_Column_List">
        vote_id
        , user_id, candidate_id, vote_time
    </sql>

    <!-- 根据Example条件查询投票记录 -->
    <select id="selectByExample" parameterType="bean.VoteRecordExample" resultMap="BaseResultMap">
        select
        <if test="distinct">
            distinct
        </if>
        <include refid="Base_Column_List"/>
        from vote_record
        <if test="_parameter != null">
            <include refid="Example_Where_Clause"/>
        </if>
        <if test="orderByClause != null">
            order by ${orderByClause}
        </if>
    </select>

    <!-- 根据主键查询投票记录 -->
    <select id="selectByPrimaryKey" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from vote_record
        where vote_id = #{voteId,jdbcType=INTEGER}
    </select>

    <!-- 根据主键删除投票记录 -->
    <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer">
        delete
        from vote_record
        where vote_id = #{voteId,jdbcType=INTEGER}
    </delete>

    <!-- 根据Example条件删除投票记录 -->
    <delete id="deleteByExample" parameterType="bean.VoteRecordExample">
        delete from vote_record
        <if test="_parameter != null">
            <include refid="Example_Where_Clause"/>
        </if>
    </delete>

    <!-- 插入投票记录 -->
    <insert id="insert" parameterType="bean.VoteRecord">
        insert into vote_record (vote_id, user_id, candidate_id,
                                 vote_time)
        values (#{voteId,jdbcType=INTEGER}, #{userId,jdbcType=INTEGER}, #{candidateId,jdbcType=INTEGER},
                #{voteTime,jdbcType=TIMESTAMP})
    </insert>

    <!-- 选择性插入投票记录 -->
    <insert id="insertSelective" parameterType="bean.VoteRecord">
        insert into vote_record
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="voteId != null">
                vote_id,
            </if>
            <if test="userId != null">
                user_id,
            </if>
            <if test="candidateId != null">
                candidate_id,
            </if>
            <if test="voteTime != null">
                vote_time,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="voteId != null">
                #{voteId,jdbcType=INTEGER},
            </if>
            <if test="userId != null">
                #{userId,jdbcType=INTEGER},
            </if>
            <if test="candidateId != null">
                #{candidateId,jdbcType=INTEGER},
            </if>
            <if test="voteTime != null">
                #{voteTime,jdbcType=TIMESTAMP},
            </if>
        </trim>
    </insert>

    <!-- 根据Example条件统计投票记录数量 -->
    <select id="countByExample" parameterType="bean.VoteRecordExample" resultType="java.lang.Long">
        select count(*) from vote_record
        <if test="_parameter != null">
            <include refid="Example_Where_Clause"/>
        </if>
    </select>

    <!-- 根据Example条件选择性更新投票记录 -->
    <update id="updateByExampleSelective" parameterType="map">
        update vote_record
        <set>
            <if test="record.voteId != null">
                vote_id = #{record.voteId,jdbcType=INTEGER},
            </if>
            <if test="record.userId != null">
                user_id = #{record.userId,jdbcType=INTEGER},
            </if>
            <if test="record.candidateId != null">
                candidate_id = #{record.candidateId,jdbcType=INTEGER},
            </if>
            <if test="record.voteTime != null">
                vote_time = #{record.voteTime,jdbcType=TIMESTAMP},
            </if>
        </set>
        <if test="_parameter != null">
            <include refid="Update_By_Example_Where_Clause"/>
        </if>
    </update>

    <!-- 根据Example条件更新投票记录 -->
    <update id="updateByExample" parameterType="map">
        update vote_record
        set vote_id = #{record.voteId,jdbcType=INTEGER},
        user_id = #{record.userId,jdbcType=INTEGER},
        candidate_id = #{record.candidateId,jdbcType=INTEGER},
        vote_time = #{record.voteTime,jdbcType=TIMESTAMP}
        <if test="_parameter != null">
            <include refid="Update_By_Example_Where_Clause"/>
        </if>
    </update>

    <!-- 根据主键选择性更新投票记录 -->
    <update id="updateByPrimaryKeySelective" parameterType="bean.VoteRecord">
        update vote_record
        <set>
            <if test="userId != null">
                user_id = #{userId,jdbcType=INTEGER},
            </if>
            <if test="candidateId != null">
                candidate_id = #{candidateId,jdbcType=INTEGER},
            </if>
            <if test="voteTime != null">
                vote_time = #{voteTime,jdbcType=TIMESTAMP},
            </if>
        </set>
        where vote_id = #{voteId,jdbcType=INTEGER}
    </update>

    <!-- 根据主键更新投票记录 -->
    <update id="updateByPrimaryKey" parameterType="bean.VoteRecord">
        update vote_record
        set user_id      = #{userId,jdbcType=INTEGER},
            candidate_id = #{candidateId,jdbcType=INTEGER},
            vote_time    = #{voteTime,jdbcType=TIMESTAMP}
        where vote_id = #{voteId,jdbcType=INTEGER}
    </update>
</mapper>